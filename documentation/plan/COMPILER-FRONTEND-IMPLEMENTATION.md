# Compiler Frontend Migration Follow-up

This is the implementation checklist for [`COMPILER-FRONTEND-ARCHITECTURE.md`](COMPILER-FRONTEND-ARCHITECTURE.md). Statuses describe repository work, not promises. A phase advances only when its acceptance checks pass; the legacy compiler remains the default until Phase 7 promotion.

## Component register

| ID | Component / deliverable | Phase | Current status | Acceptance / expiry |
|---|---|---:|---|---|
| SRC-01 | Source identity, immutable snapshots, revisions, UTF-8/UTF-16 coordinate map | 0 | Complete | Unit tests cover Unicode boundaries, revisions, canonical file loading; the full CLI suite passes unchanged. Becomes foundation for every parser and diagnostic. |
| PAR-01 | Parser backend/result/selection contract | 1 | Contract and explicit parser selection are implemented; shared-subset differential check passes, broad gate open | Generator-neutral nodes/results, registry selection, CLI `parse --backend`, and explicitly partial legacy scanning are covered by tests. Shadow comparison checks normalized comptime/test construct names and exact spans; broad source/diagnostic/output parity remains open. Legacy adapter expires only after new backend promotion. |
| TS-01 | Pinned Tree-sitter C grammar/runtime integration | 2 | Host-specific JNI resources, six-host artifact aggregation, and release-JAR resource checks are configured; Linux x86_64/JDK 21 verified locally, remote matrix pending | Reproducible generation, Java 21-compatible loading/package proof on each supported host, license/provenance, CI parser build. |
| TS-02 | C-plus grammar extension and corpus | 2 | Explicit grammar slices for methods, defer, comptime, tests, and error handling | Named nodes for supported constructs; all upstream C and C-plus corpus passes. |
| AST-01 | Stable C-plus CST adapter and AST schema | 3 | Core C/C-plus nodes now include distinct enum/declaration/field/parameter/argument containers, declarator-binding-aware function classification, and normalized annotations/type parameters; full-language normalization remains open | Adapter coverage, span fidelity, unknown/recovery preservation, grammar upgrade tests. |
| MOD-01 | Canonical source/import graph and C/C-plus boundary | 4 | Shared `SourceManager`, deterministic graph ordering, canonical edges, confined path resolution, and CLI/VS Code/IntelliJ/Vim graph consumers are implemented; Tree-sitter prototype now expands AST-recognized C and C-plus imports with mapped origins and cycle diagnostics; broader editor parity remains open | Relative/module/stdlib resolution, canonical identity, cycle diagnostics, C includes unchanged. |
| CT-01 | AST-backed comptime parsing/materialization | 4 | AST syntax index records generator signatures/result kinds and invocation arguments/aliases; experimental Tree-sitter pipeline materializes bounded OS conditionals, integer/boolean scalar values, compiler flags, and a bounded C-plus module-import graph; general generator/reflection evaluator remains legacy | Existing limits/security retained; generated declarations reparse to fixed point; multi-pass maps preserved. Text scanners expire after parity gate. |
| SEM-01 | Scope, symbols, method/type resolution, annotation metadata | 5 | Struct/method resolution tracks ordered pointer/array/function declarator layers, receiver access-operator and direct declarator-shape compatibility (including struct-array decay), comma-separated declarations, throws metadata, nested callback signatures, dormant comptime exclusion, and module-scoped comptime generator bindings. Full C/comptime semantics remain open. | Qualified/deeper declarators, materialized/local comptime scopes, argument type/evaluation semantics, and broad diagnostics; ownership remains advisory. |
| IR-01 | Source-spanned runtime IR and ordered lowering pipeline | 6 | Reparse-between-pass AST pipeline implemented and used by the prototype's runtime lowering segment; explicit typed lowered-node IR remains open | Pass order is recorded, changed output is reparsed before the next pass, and parse/lowering failures stop the pipeline; richer typed lowered nodes and no-comptime-node promotion gates remain open. |
| LOWER-01 | AST lowering for methods, defer, try/catch, tests, allocation diagnostics | 6 | Opt-in AST pipeline now lowers validated throws contracts and statement-oriented checked calls/try-catch, in addition to methods and defer; allocation analysis models sequenced comma, conservative conditional/call-argument joins, annotated output calls, and loop/switch-write invalidation. Broader parity remains open. | Differential output and behavior parity. Test harness extraction, broad defer scopes, general expression/loop fixed-point analysis, and legacy retirement remain open. |
| EMIT-01 | Structured C emitter, origin segments, diagnostic mapping | 7 | Experimental pipeline now emits lowered output from normalized AST terminals with mapped token origins; full grammar-aware formatting and host matrix open | Supported output compiles; directives are preserved; parser recovery fails closed; mapped diagnostics cover lowered code. |
| IDE-01 | Shared parser-backed editor indexing/diagnostics/test discovery | 8 | Versioned normalized-AST JSON contract, CLI `parse` bridge, and opt-in VS Code/Vim/IntelliJ parser-diagnostics consumers implemented; shared symbol consumers remain open | Existing editor capabilities preserved, incomplete buffers recover, CLI diagnostics agree with editor locations. |
| VERIFY-01 | Differential, fuzz, compatibility, performance, platform suite | 0–8 | Initial legacy-vs-Tree-sitter runtime and source-map differential fixture added; broad suite open | Runs continuously; gates parser promotion and each legacy retirement. |
| RETIRE-01 | Legacy backend/lowerer removal | 9 | Not started | New backend default for two stable releases, no open corpus mismatches, rollback documented, all component gates passed. |

## Phase sequence

### Phase 0 — source foundation (current)

- [x] SRC-01: establish source IDs, snapshots, revisioning, and exact UTF-8/UTF-16 boundary conversion.
- [x] Integrate imported comptime sources with `SourceManager` while retaining `SourceFile` compatibility; tests verify the imported snapshot is registered.
- [x] Own resolved comptime edges in `SourceImportGraph`, provide deterministic dependency order, and map cycle diagnostics to the root import request; tests cover DAG order, cycle closure, and root-span reporting.
- [x] Lock a representative legacy transpilation to a byte-for-byte C golden and assert its generated statement maps to the original `.cp` line; imported-source tests continue to cover `#line` origin changes.

### Phase 1 — switchable frontend contract (shadow comparison prototype implemented)

- [x] Define parser input/options/result and backend ID without generator-specific node types.
- [x] Add registry selection with `legacy` as default; no CLI behavior or generated C changes.
- [x] Expose explicit `parse --backend legacy|tree-sitter` selection for parser diagnostics/AST comparison, preserving Tree-sitter as the existing parse-command default and leaving compilation untouched.
- [x] Adapt the existing comptime scanner's recognized items to neutral syntax nodes; explicitly label all other text as opaque.
- [x] Add a non-invasive shadow runner that returns the authoritative parse unchanged and reports backend coverage, diagnostics, normalized-node fingerprints, and shared comptime/test construct-span parity. Differential fixtures cover comptime values, flags, imports, C imports, function/type-result declarations, type invocations, and tests.
- [x] Run shared-construct/span parity across every `.cp`/`.c+` file under `stdlib` and `examples` (62 repository files at verification time); descend through preprocessor wrappers while treating comptime blocks/declaration bodies as one expansion unit. Canonicalize trailing whitespace from legacy scanner spans before comparing syntax ranges. The broader corpus exposed and fixed normalization gaps for legacy `@fn` declarations, `typedef @generator(T) Alias`, and bare `@generator(...)` invocation statements. This does not assert whole-tree/output parity.
- [x] Differentially reject malformed shared comptime function, flags, import, block, test, and legacy type-generator constructs in both parsers; require source-contained error spans and cross-backend error-location alignment, including zero-width missing-token insertion points. Exact ranges/messages and whole-language diagnostic parity remain open. This gate exposed and fixed a legacy unclosed-`@type` body error previously anchored at file offset zero.
- [x] Execute the same method/receiver fixture through both transpilers and a host C11 compiler; compare exit status, exact stdout, and generated method source-map locations. This starts runtime output comparison for the supported subset; broader source, diagnostic, and behavior parity remain open.

### Phase 2 — grammar and runtime prototype (grammar slice complete; runtime open)

- [x] Pin upstream `tree-sitter-c` revision and license; vendor it under the dedicated parser module with a clear local patch log.
- [x] Select KTreeSitter 0.25.1 and verify generated grammar loading under Java 21 on Linux x86_64.
- [x] Measure the local Linux x86_64 parser artifact and verify its JNI resource is present in the packaged JVM jar: 149 KiB jar, containing a 1,283,272-byte `libktreesitter-c.so`; JVM integration tests load and parse through the native bridge.
- [ ] Confirm JNI loading and packaged sizes on every supported Linux, macOS, and Windows host architecture; CI is configured, but those remote host results are not available from this local run.
- [x] Install the JNI library at the runtime loader's OS/architecture resource path and configure CI smoke tests for Linux/macOS/Windows x86_64 and arm64; actual remote runner results remain pending.
- [x] Upload each host-specific grammar JNI library from the six-host CI matrix, merge them into the CLI's parser resources before bundling, and verify both release JARs contain exactly the six supported grammar-bridge paths. Remote artifact assembly remains to be confirmed by a workflow run.
- [x] Extend C grammar for one vertical slice: access-annotated methods in structs and annotated/inferred receiver parameters. Receiver calls already parse through standard C field/call nodes.
- [x] Add the next explicit statement slice: `defer statement` and `defer { ... }`, with a valid-source corpus case.
- [x] Add comptime declaration/invocation/import/flags/block, `@test`, `@throws`, and `@try`/`@catch` syntax nodes with dedicated corpus coverage.
- [x] Add an explicit AST node for platform-conditioned `@if` / `@else if` / `@else` blocks, including comptime flags in selected branches.
- [x] Add generated-parser reproducibility check and grammar tests; valid C-plus syntax uses explicit rules rather than recovery nodes.
- [x] Extend generated-artifact reproducibility verification to compare `grammar.json`, `node-types.json`, and `parser.c` against the pinned grammar generation, so stale parse metadata cannot pass while the parser source happens to match.
- [x] Parse both marked and unmarked comptime value names, and keep inline comptime expressions in expression contexts without letting them steal top-level declaration parses; corpus and shadow tests cover both.
- [x] Parse the legacy `@type @name(@type T) { ... }` generator form as a named comptime type-generator node and index its symbol/body span; the legacy parser remains authoritative for evaluation.
- [x] Parse legacy `typedef @generator(T) Alias;` specializations, `@fn @generator(@type T)` declarations, returned `@fn` definitions, and `@T` type references explicitly; parser fixtures cover the generic-list example syntax.
- [x] Recognize allocator-domain/ownership annotations in declarations, local variables, and cast type descriptors; access annotations on file-scope declarations; qualified untyped pointer receivers; direct declaration returns from comptime functions; and the Win32 `WINAPI` calling-convention macro. JVM integration gates parse every `.cp`/`.c+` source under `stdlib/` and `examples/` without recovery.
- [x] Parse `@assert(...)` and `@assertEquals(...)` as explicit statement nodes; AST test extraction accepts these and the legacy at-call representation.
- [x] Parse identifier splices such as `list_of_@typename(T)` and `mapper__@typename(T)__to__@typename(R)` as explicit interpolation nodes, including when used as generated type names; corpus and stable-AST/JVM tests cover them. Remaining dialect edge cases stay separate from this completed slice.

### Phase 3 — normalized AST (common C/C-plus declaration slice implemented)

- [x] Define stable AST categories, syntax-kind preservation (including interpolated identifiers), and source-span conventions independent of Tree-sitter; verify serialization through `cplus.parse.v1`.
- [x] Adapt translation units, structs/unions/enums, typedefs, fields, variables, function definitions/prototypes, methods, parameters, blocks, calls, field access, control flow, preprocessor directives, comptime/test/error-handling nodes, and error nodes. Full C/comptime normalization remains open.
- [x] Add deterministic AST dump support and golden-style assertions for reviewable parser changes.
- [x] Test supplementary-Unicode spans and malformed-buffer diagnostics against SRC-01 coordinate conventions.
- [x] Normalize explicit `@assert` / `@assertEquals` grammar nodes to a stable test-assertion AST category and verify syntax kind/span preservation.
- [x] Normalize comptime declaration/generator, invocation, inline-expression, and code-fragment nodes to generator-neutral AST categories; adapter tests pin categories while retaining original syntax kinds.
- [x] Normalize legacy `@fn` generators, returned `@fn` function declarations, and `typedef @generator(T) Alias;` specialization nodes while preserving their concrete syntax kinds and source spans.
- [x] Give common C declarators, initializers, enumerators, attributes, and designators explicit stable AST categories; the C/JVM integration fixture verifies these alongside their original syntax kinds.
- [x] Classify declarations by the binding operation nearest each declared identifier, rather than by the presence of any nested function declarator. Prototypes and functions returning pointers remain function declarations, while function-pointer objects and mixed function/object declarations are not mislabeled as function-only declarations; regression coverage includes a function returning a function pointer.
- [x] Normalize C parameter and call argument lists as dedicated stable AST container kinds, preserving their original syntax names, child nodes, and source spans for semantic and comptime passes.
- [x] Preserve the distinction between an enum's `enumerator_list` container and each `enumerator` entry, with adapter assertions pinning both categories.
- [x] Normalize common C field/declaration-list and linkage containers, `else`/SEH control-flow, variadic-parameter, bit-field, concatenated-string, GNU-assembly, and preprocessor-helper nodes into their stable AST families; retain concrete `syntaxKind` values.
- [x] Normalize C-plus access, static, ownership/mutation, and allocator-result annotations as `ANNOTATION`; normalize generic type parameters/references and comptime bodies/return declarations to stable type, block, and statement kinds.
- [x] Classify attributed statements as `STATEMENT` instead of leaving them in `OTHER`, while retaining their concrete grammar kind; the common-C adapter fixture verifies `[[likely]] if (...)` produces the wrapper category.
- [x] Normalize comments as `COMMENT` and goto-label identifiers as `IDENTIFIER`; the common-C fixture verifies both without discarding their original syntax kinds or spans.
- [x] Audit every named node in the pinned `node-types.json` against the adapter; the JVM test fails if any named grammar node silently falls through to `OTHER`.

### Phase 4 — imports and comptime

- [x] Load imported C-plus files through the shared `SourceManager` and record canonical directed import edges with requesting spans; retain `#include` for C compiler handling.
- [x] Expose deterministic dependency-first ordering from the source graph; reject cycles rather than returning a partial order.
- [x] Expose the dependency-first graph order and canonical import edges (including requester spans) to compiler API consumers through `TranscodedSource`.
- [x] Add the versioned `cplus graph` JSON projection and VS Code/IntelliJ **Show Import Graph** actions that display resolved edges and open an imported source; compiler/CLI and IntelliJ tests verify graph decoding and canonical identities, edge spans, and dependency-first order.
- [x] Verify relative, `stdlib:/`, `module:/`, and `project:/` imports, extensionless `.cp` resolution, dependency-first graph membership, and configured-root traversal rejection; existing-path symlink aliases are covered by a host-capability-aware `SourceId` canonicalization test.
- [x] Share import path resolution between the legacy comptime evaluator and the Tree-sitter prototype; lower AST-recognized `@import("file.c")` into an absolute C `#include`, preserving the importing source as the generated directive's origin. Relative and `stdlib:/` imports compile/run in the prototype, and missing-file diagnostics map back to the `.cp` import.
- [x] Expand module-scope modern `comptime import` and legacy `.cp`/`.c+` `@import` nodes through a canonical AST-driven dependency graph; reject nested runtime imports, keep generator-body imports dormant, deduplicate modules, reject cycles at the requesting source span, select supported OS conditionals before resolving imports, emit dependencies before importers, preserve per-module source origins, and expose graph metadata on `TranscodedSource`. Nested relative/`stdlib:/` imports compile and run, and imported scalar comptime declarations participate in later materialization.
- [x] Reuse resolved dependency/import graph metadata in the VS Code import-graph action; it invokes `cplus graph`, lists dependency edges, and opens the selected imported file.
- [x] Reuse resolved graph metadata in VS Code and IntelliJ actions; both invoke `cplus graph`, list dependency edges, and open the selected imported file.
- [x] Add Vim `:CPlusImportGraph` presentation for resolved edges using a navigable location list; preserve the diagnostics quickfix list and cover it with a headless CLI integration test.
- [x] Verify live unsaved-buffer parsing in VS Code and Vim through the shared stdin parse contract; Vim headless coverage edits a buffer without saving before requesting diagnostics and AST symbols. Import graphs remain disk-backed and require saved sources.
- [ ] Verify broader editor feature parity for import graph presentation, completion, and diagnostics behavior.
- [x] Index comptime declarations, legacy `@fn` generators, typedef specializations, invocations, imports, generator bodies, platform conditionals, and tests from AST nodes; nested declarations in generator bodies are marked dormant until materialization. Evaluator semantics and resource limits remain unchanged.
- [x] Preserve the nearest enclosing generator span on each indexed comptime construct, so dormant nested generators and invocations retain their expansion-tree parent instead of being flattened into an unowned list.
- [x] Preserve generator result kinds and parameter names/types, plus invocation argument spans and typedef aliases, in the AST index for future evaluator migration. Modern and legacy generic forms are tested without changing evaluation behavior.
- [x] In the experimental Tree-sitter pipeline only, materialize structural `@if` / `@else if` / `@else` conditions of the form `os == "..."` or `os != "..."`; normalize target OS names, select one branch, reparse nested selected branches to a fixed point, and preserve selected-text origins. Unsupported condition expressions fail with mapped diagnostics. This is not the general comptime evaluator and does not change the production legacy path.
- [x] Add AST-only scalar materialization for comptime integer/bool declarations and inline `comptime` expressions. It resolves forward scalar dependencies, evaluates integer arithmetic, comparisons, bitwise/logical operators, honors logical short-circuiting, removes compile-time declarations, maps emitted literals back to the expression source, and diagnoses duplicate/cyclic/unsupported values and arithmetic faults. This remains an opt-in prototype subset; generators, strings, reflection, and imports still use or require the legacy path.
- [x] Extract active module-scope `comptime flags` from the AST after platform branch selection, preserve argument grouping for quoted flags, deduplicate in first-seen order, remove directives before C emission, and expose the result on `TreeSitterPrototypeResult.compilerOptions`. Reject runtime-function flag directives rather than silently treating them as compile-line options.
- [x] Reparse each emitted declaration layer and preserve source origins through every iteration; the nested-generator CLI integration test materializes declarations across passes and asserts the final generated method maps to its original `.cp` source.
- [ ] Retire old comptime scanners only after all current spec examples/tests pass through both paths.

### Phase 5 — resolution and semantics

- [x] Build a first semantic index for struct/union names, fields, functions, instance/static methods, method parameters, access, and ownership/mutation annotations. Local block variables support simple explicit receiver typing.
- [x] Resolve unambiguous `value.method()` / `pointer->method()` and `Type.static_method()` calls by declared receiver/type symbols.
- [x] Infer the receiver type of an untyped `self` parameter from its enclosing struct, so calls such as `self->value()` inside instance methods resolve through the same method index.
- [x] Resolve explicit parenthesized address/dereference receivers such as `(&value).method()` and lower an already-pointer receiver without taking its address twice; compile/run the generated C regression.
- [x] Resolve chained field receivers such as `holder.child.method()` and `self->child.method()` from declared field types, then compile/run the lowered C.
- [x] Resolve explicit instance receivers in type-qualified form such as `Type.method(&value, ...)`; distinguish them from static calls by resolving the first argument's pointer type, lower the explicit receiver exactly once, and compile/run the emitted C.
- [x] Seed receiver resolution from file-scope variable declarations, including declarations within preprocessor branches, while excluding typedef declarations from value scope; this enables prototype lowering of methods on globals such as the xmem singleton.
- [x] Register each object declarator in comma-separated file- and block-scope declarations, unwrap initialized declarators, preserve each declarator's pointer depth, and avoid treating ordinary function declarations as value objects; Tree-sitter semantic regression covers global/local values and pointers.
- [x] Give `for` statements an isolated declaration scope: initializer variables are visible in the loop condition, update, and body, then disappear after the loop; the regression verifies receiver calls in all three loop regions and excludes an out-of-scope use.
- [x] Preserve C declarator binding order for pointer, array, and function layers through variables, aliases, parameters, and fields. Resolve indexed receivers for value arrays, arrays of pointers, pointer-to-array and multidimensional indexing, and array fields; compile and execute the generated fixture with installed host C compilers.
- [x] Validate resolved instance-call syntax against the known receiver type: `.` for values, `->` for one-level pointers, preserve explicit `(&value).method()`, and require explicit dereference for pointer-to-pointer receivers. Emit mapped semantic diagnostics and fail the prototype pipeline closed on mismatches.
- [x] Validate the complete ordered declarator shape at direct receiver sites, not only total pointer depth: permit C array-to-pointer decay for arrays of structs, reject direct `->` on arrays of pointers and pointers to arrays, and require indexing/dereferencing to select a struct value or direct pointer. Compile/run the array-decay lowering case and verify invalid-shape diagnostics retain exact spans.
- [x] Preserve `@throws()` versus `@throws(error)` as distinct semantic metadata for global functions and methods, including their parameter symbols.
- [x] Resolve named typedef chains, const-qualified aliases, pointer typedefs, and typed parameters for receiver calls; local blocks shadow outer variables without leaking symbols.
- [x] Track pointer indirection introduced by typedefs, variables, parameters, and fields; avoid resolving implicit methods through pointer-to-pointer receivers while allowing an explicit dereference to restore a single-pointer receiver.
- [x] Associate anonymous `struct`/`union` bodies with their typedef names so their fields and methods participate in receiver-call resolution.
- [x] Extract catch alternatives and catch-all error binding names/types as structured semantic data.
- [x] Index function-pointer typedefs as callable signatures (base result type, parameter names/base types and original parameter spellings, variadic marker, declarator span); retain the complete typedef declaration verbatim so pointer/qualifier placement remains available until fully normalized, and exclude parameter-list identifiers when resolving the typedef name.
- [x] Preserve recursively nested callable signatures when a function-pointer parameter itself accepts callback parameters, and record function-pointer return composition; an integration fixture parses, indexes, compiles, and executes both forms.
- [x] Resolve active comptime invocation/type-specialization nodes to active AST-indexed generators by symbol and arity; diagnose unresolved generators, wrong arity, duplicate same-arity declarations, and ambiguous matches with source spans. Generator declarations and calls inside dormant generator bodies remain excluded. The opt-in pipeline maps resolution diagnostics back to the originating `.cp` source; argument type checking, evaluation, and expansion remain evaluator responsibilities.
- [x] Restrict visible generator declarations to module scope while allowing module generators to be invoked inside runtime functions; a generator declared inside a runtime function does not leak into or satisfy the module namespace. Dormant comptime closure contents remain deferred to materialization.
- [x] Exclude unmaterialized comptime definitions (including legacy `@fn` generators), typedef specializations, invocations, code fragments, blocks, and conditionals from runtime symbols, receiver resolution, and catch-binding indexes; generated declarations become visible after expansion and reparsing.
- [x] Diagnose static methods called through instances and instance methods called through type names; expose the receiver-kind mismatch through the experimental lowering pipeline with the original call span.
- [ ] Complete deeper nested/qualified declarator compatibility, less common qualified C typedef forms, and comptime name/scope resolution; other invalid receiver forms remain open.
- [x] Add an end-to-end C compatibility test for a function-pointer typedef, conditional preprocessing, MSVC/GNU API attributes, and host C compile/run.
- [x] Run the C11 function-pointer/declarator compatibility fixture through each locally available `cc`, `gcc`, and `clang` driver and execute each output; drivers absent on a host are skipped.
- [ ] Expand C compatibility verification across supported target compilers/dialects and additional declaration ambiguities; cross-host and cross-target compiler coverage remains open.
- [ ] Keep unimplemented C semantic checks delegated to the selected C compiler; do not claim whole-language type checking prematurely.

### Phase 6 — runtime IR and lowering

- [x] Introduce source-spanned lowering results, mapped edits, and explicit diagnostics; build the rest of the ordered IR pipeline incrementally.
- [x] Add a reusable ordered AST-lowering runner that checks AST/source revision agreement, reparses every changed pass result, records pass traces, and stops on either lowering or parser diagnostics; migrate the experimental runtime-lowering sequence to this runner without changing the legacy default.
- [x] Port function-scoped `defer` to an AST pass with reverse ordering and origin preservation.
- [x] Lower resolved value, pointer, and static receiver calls to C function calls using source-spanned AST edits.
- [x] Extract struct-contained methods into ordinary C functions, add the receiver's concrete type, and preserve mapped origins.
- [x] Extract global and method `@throws` conventions from AST declarations, validate required signatures, and remove annotation spans with mapped AST edits.
- [x] Lower standalone checked calls and error-out assignment/initializer values inside AST `@try` bodies; support nested propagation, ordered catches, explicit manual error pointers, and mapped diagnostics.
- [x] Extract AST `@test` fixtures from ordinary runtime C, preserving decoded names, mapped bodies, and declaration spans.
- [x] Generate and run the test harness from AST-extracted fixtures through a compiler bridge; runtime compilation verifies assertion lowering and fixture execution.
- [x] Replace scanner-based assertion discovery/lowering for AST-extracted fixtures with source-spanned AST records for `@assert` and `@assertEquals`; compile and run both through the existing harness, verify assertion-like string contents are ignored, and report bad arity at the original call span. The legacy comptime fixture path retains its scanner until broader parity is proven.
- [x] Expand AST `defer` collection to nested control-flow blocks while retaining function-closure-wide LIFO placement; compiled C executes a nested-defer fixture and checks reverse group order and preserved in-group order.
- [x] Emit supported method headers directly from Tree-sitter declarator/parameter nodes: access/static modifiers, return declarator prefix (including pointer returns), generated C name, inferred typed `self`, remaining parameters, and original method body/prototype. Parser integration tests compile and execute instance, static, annotated, pointer-returning, and prototype methods.
- [x] Add an opt-in Tree-sitter AST allocation-intent slice for direct `alloc_*`/`calloc_*`/`realloc_*` initializers, simple lexical initializer aliases, standalone direct identifier assignments, annotated global-function parameter checks at call sites, and annotated global-function return checks; merge matching prototype/definition contracts and expose parameter/return/variable symbols plus identifier-mapped mismatches without changing the production analyzer.
- [x] Recognize `owned` double-pointer output parameters in global functions, diagnose mismatched allocator domains written through `*out`, and exclude those output parameters from input-argument checks.
- [x] Check allocation-domain annotations on resolved instance and static method parameters against call argument provenance, using semantic receiver/type resolution rather than name-only matching.
- [x] Check annotated method return allocation intent against direct allocator returns and known parameter/alias provenance, retaining method return ownership metadata and mapped return-expression diagnostics.
- [x] Propagate annotated function/method return provenance through direct call expressions into enclosing argument and return checks; keep inference limited to explicit contracts and known allocator families.
- [x] Merge outer-variable provenance across `if/else` only when both paths agree; when `else` is absent, include the unchanged incoming path. Block-local declarations remain scoped.
- [x] Infer allocation provenance from a conditional (`?:`) expression only when both value arms are known and agree; treat different or unknown arms as unknown.
- [x] Infer allocation provenance from assignment and comma expressions in value contexts, and propagate side effects for ordered comma operands; assignment diagnostics are emitted at the destination identifier.
- [x] Join allocation provenance by domain across `if/else` and conditional expressions, invalidate known values when branch outcomes disagree, and conservatively merge the possibly skipped RHS of `&&`/`||`. Function-call arguments are analyzed on independent snapshots; conflicting possible writes invalidate prior provenance. Unsequenced binary operands are not assigned a guessed evaluation order; this is not a complete C expression evaluator.
- [x] After one diagnostic walk of `while`, `for`, or `do` bodies, invalidate provenance for outer variables assigned in the loop; keep `for` initializer locals scoped to the loop. This prevents stale pre-loop assumptions without claiming fixed-point evaluation.
- [x] Analyze each `switch` case from an independent conservative snapshot, and invalidate outer-variable provenance for names assigned in any case or passed by address to an annotated owned double-pointer output parameter; case skipping and fallthrough are not treated as a single deterministic path.
- [x] Treat `&variable` passed to an annotated owned double-pointer function/method parameter as a possible caller-side write; preserve a known domain only when it agrees with the incoming domain, otherwise invalidate it. Align allocation-contract checking for explicit type-qualified instance calls by excluding the explicit receiver from user-parameter matching.
- [ ] Implement loop fixed-point analysis and broader expression-aware side-effect/order semantics; keep the legacy path until parity gates pass.
- [ ] Complete broad receiver inference and cover complex/qualified method declarators; unsupported AST shapes must continue to fail closed.
- [ ] For each pass, compare runtime behavior, C compiler output, source maps, and failure locations with legacy behavior.
- [ ] Retire each textual lowerer independently only after its own corpus gate; keep the others active meanwhile.

### Phase 7 — emitter and promotion

- [x] Add an opt-in prototype pipeline that composes test-fixture extraction, throws extraction, statement-oriented try/catch, defer, receiver-call, and struct-method passes, reparsing between structural stages; fail closed on comptime and C-plus imports.
- [x] Keep newly structured platform comptime conditionals inside that fail-closed boundary; regression checks the unsupported-node kind and original source span so raw `@if` cannot leak into generated C.
- [x] Compile and execute the emitted method/defer subset with host `cc`; production `CPlusTranspiler` remains untouched.
- [x] Expose prototype output through the existing `TranscodedSource`/source-map facade in addition to its mapped-text result; compile/run mapped emission and assert that a lowered method line maps to its original `.cp` declaration.
- [x] Add a node-keyed mapped AST emitter for replacements and before/after-node insertions; use it for fixture extraction, `@throws` removal, `defer`, struct-method extraction, and receiver-call lowering (including recursive nested calls), preserving inter-node trivia and mapped origins. This is a structured source-preserving transition, not yet canonical AST-to-C generation.
- [x] Emit outer/nested `@try` expansions and checked-call statement rewrites as node-keyed replacements in each mapped AST subregion; preserve untouched trivia and source-origin segments through nested handler bodies. Runtime C11 coverage verifies nested propagation and generated behavior.
- [x] Add `CPlusAstCEmitter` to emit terminal tokens from normalized AST nodes, generate deterministic brace/statement layout and indentation, use AST context for control-keyword parentheses and conditional-expression colons, handle semicolons inside parenthesized `for` headers, preserve preprocessor regions/token origins, and fail closed for recovered/opaque or unlowered C-plus trees; use it for the opt-in prototype's final C stage and reparse that output before returning it. Tests verify token-stream round-trip, host C11 compile/run, and mapped diagnostics.
- [x] Emit each C string, character, and number literal as one source-spelled token. A legacy-vs-Tree-sitter executable comparison caught whitespace inserted inside a `printf` format string; the fixture now covers escaped character literals and adjacent C string literals, and checks exact stdout.
- [ ] Replace terminal serialization with grammar-aware C formatting and semantic node generation where transformations synthesize declarations; expand emission validation across the supported C/C-plus corpus before production promotion.
- [x] Exercise `#line` diagnostic fidelity with installed host TCC, GCC, and Clang; source-mapped tests compile intentionally invalid generated method C and verify the original `.cp` file/line.
- [ ] Run mapped-diagnostic checks across the supported host CI matrix and compare the compiler facade's structured diagnostics; retain `#line` until verified there.
- [x] Add an opt-in `:parser-tree-sitter:benchmarkFrontend` task measuring fresh-parser batches for all current stdlib/example sources, generated large-C parsing, generated large-C-plus prototype transpilation, and fresh full reparse after an edit; exclude the tagged benchmark from ordinary tests.
- [x] Compile the prototype-generated C for `stdlib/io/file.cp` and `stdlib/memory/xmem.cp` with locally available C11 compilers; the regression also checks singleton method lowering, not just parser acceptance or successful transpile status.
- [x] Extend opt-in transcode timings to supported real stdlib and example modules on the local host; preserve results and failed candidates rather than counting parse-only timing as transcode coverage.
- [x] Add a per-document Tree-sitter parse session that applies a source edit to the previous tree and reparses with `oldTree`; test repeated Unicode/malformed edits against fresh parse trees and diagnostics, then measure tree reuse separately from full reparse.
- [ ] Run representative-source transcode and incremental-tree measurements across the supported host CI matrix; account for the normalized-AST adapter still rebuilding from the reused CST.
- [ ] Promote Tree-sitter backend to default only after grammar, semantic, output, diagnostics, performance, and packaging gates pass.

### Phase 8/9 — IDE adoption and retirement

- [x] Preserve IDE-specific adapters and provide C-plus token/completion coverage for annotations, `self`, comptime forms, `defer`, and checked-error forms without coupling plugins to Tree-sitter internals.
- [x] Define versioned editor-facing normalized-AST JSON (`cplus.parse.v1`) with UTF-16 offsets, stable syntax/category fields, recovery flags, and source-mapped diagnostics.
- [x] Expose the parser-neutral JSON contract through `cplus parse file.cp [-o ast.json]`; emit recovered AST plus diagnostics even on invalid input, with a nonzero status for syntax errors. Verify CLI runtime native loading and JSON content.
- [x] Add opt-in VS Code consumption of `cplus.parse.v1` diagnostics on save; convert UTF-16 offsets with `TextDocument.positionAt`, ignore stale document revisions, and retain existing local/compiler diagnostics.
- [x] Add Vim `:CPlusParse` and optional parse-on-write quickfix integration consuming `cplus.parse.v1`; convert UTF-16 columns to Vim byte columns and test clean and malformed buffers headlessly.
- [x] Add opt-in IntelliJ external annotations backed by the configured CLI parser; parse the current unsaved buffer, bound process execution, decode versioned diagnostics, and test JSON/schema mapping.
- [x] Reuse normalized parser results for the VS Code outline when a current `cplus.parse.v1` tree is cached; retain the legacy index as a stale/unavailable-parser fallback and keep UI types independent of Tree-sitter nodes.
- [x] Reuse an exact-current VS Code parser tree for Testing view fixture discovery and source ranges; refresh discovered tests when asynchronous parsing accepts a new tree, with the source scanner as the stale/unavailable fallback.
- [x] Reuse normalized parser results in Vim through `:CPlusSymbols`, projecting structs, fields, methods, and functions into a navigable location-list outline with UTF-16-to-byte column conversion, preserving the quickfix diagnostics list, and a headless CLI integration fixture.
- [x] Retain the IntelliJ external annotator's normalized AST in a bounded, exact-source cache and use its struct/member nodes for instance/static member completion; fall back to the local text index when the parser cache is stale or missing.
- [x] Use cached normalized-AST symbol spans in IntelliJ Go to Declaration for types, members, and functions, while retaining the previous text lookup when no exact-snapshot parser result exists.
- [x] Register IntelliJ's native Structure view over cached normalized-AST types, fields, methods, and functions; navigate entries to source spans and refresh when the accepted exact-source tree changes. The view requires the configured parser command and does not invent symbols when no current tree exists.
- [x] Drive IntelliJ test-gutter fixture names and offsets from the exact-source cached normalized AST; retain the legacy scanner only when parser output is unavailable or stale. Tests cover decoded string names, AST spans, and snapshot-cache invalidation.
- [x] Consume the resolved import graph from Vim using the shared `cplus.imports.v1` CLI contract; a headless Vim test verifies the imported target is navigable and parser diagnostics remain in quickfix.
- [ ] Verify feature parity for syntax errors, outline, methods, completions, test gutters, and source-mapped diagnostics.
- [ ] Keep legacy backend opt-in for two stable releases after promotion; then remove it only after mismatch backlog is zero and rollback guidance is published.
- [ ] Remove superseded text scanners/lowerers and update specs, README, examples, changelog, and contributor instructions.

## Verification ledger

- Source snapshots remain parser-independent; Phase 1 adds backend selection, while the legacy backend remains the default and is not wired into transpilation yet.
- Phase 1 contract classes are in `compiler/.../FrontendParser.kt`; the legacy partial adapter reuses `ComptimeParser` and is not a claim of full C/C-plus parsing.
- Phase 1 verification: `CPlusParserShadowRunner` compares the legacy authoritative result against the selected shadow backend without replacing it. In addition to full-tree fingerprints, it normalizes recognized comptime/test declaration names (including marked `@` names) and compares syntax spans for values, flags, comptime imports, C imports, function declarations (including `type` result kind), type invocations, and test declarations. The legacy scanner may include trailing whitespace in an item span; the shadow comparison trims only that trailing trivia, without changing either parse result. Unit snippets and all 62 current `stdlib` plus `examples` C-plus sources pass shared-node parity. The expanded corpus exposed normalization omissions for `@fn`, generic typedef specializations, and top-level bare generator calls; these are now normalized from their Tree-sitter CST structure. Malformed comptime function/flags/import/block, `@test`, and legacy `@type` fixtures are rejected by both backends with source-contained error spans, but diagnostic codes/messages and exact range parity are not claimed. Full differential acceptance remains open until broader C-plus syntax, diagnostics, and emitted behavior are compared.
- Phase 1 malformed-diagnostic verification: `CPlusParserShadowRunner.errorLocationsAlign` requires each backend's errors to overlap the other backend's ranges (or zero-width insertion points), while leaving exact ranges, codes, and messages independent. All malformed shared-form fixtures pass; a synthetic negative control confirms disjoint errors fail the metric. The check exposed a legacy `@type` missing-body diagnostic at offset zero; the scanner now points to the expected closing-brace position at EOF. Focused parser tests and the full `gradle test --no-daemon` suite pass.
- Initial runtime differential verification: focused fixtures compile both legacy and Tree-sitter C with the local C11 compiler and compare exact stdout and exit status for method/receiver lowering, reverse-order `defer`, and successful plus caught checked calls. The method fixture also maps `counter__add` to the same `.cp` line. Its format-string output exposed inserted whitespace inside Tree-sitter string tokens; emitting string, character, and number literals as atomic source spellings fixed it. Adjacent string and escaped character literals are covered. These fixtures do not establish full corpus or host-matrix parity.
- Phase 1 CLI-selection verification: `cplus parse --backend legacy` and `--backend=tree-sitter` both emit the same versioned JSON contract while identifying the selected backend; the legacy JSON retains opaque coverage instead of implying full C parsing. The default remains Tree-sitter, and `transcode`/`compile`/`run` remain on the legacy production compiler.
- Phase 2 grammar verification: upstream `tree-sitter-c` v0.24.2 at `b780e47fc780ddc8da13afa35a3f4ed5c157823d`; Tree-sitter CLI 0.25.10 / ABI 15; all 91 upstream+extension corpus cases pass, and generated parser source matches regeneration. Extension cases cover methods, defer, marked/unmarked comptime values, inline comptime expressions, imports, tests, platform-conditioned `@if` chains, `@throws`, `@try`/`@catch`, comptime identifier interpolation, and nested function-pointer declarators.
- Phase 2 repository-syntax verification: the JVM integration gates parse every `.cp`/`.c+` source under `stdlib/` and `examples/` without Tree-sitter recovery. Corpus-backed cases include legacy `@type`, `@fn`, `@T`, and typedef specialization syntax; type-valued comptime invocation arguments; assertion statements; file-scope access annotations; allocator-domain annotations and casts; qualified untyped pointer receivers; direct declaration returns from comptime functions; and `WINAPI`. This gate does not claim whole-language C/C-plus parity.
- Latest verification: legacy-output golden, interpolation/AST, import-namespace, nested callback-signature, verbatim typedef-spelling, AST allocation-contract, explicit address-receiver, field-typed receiver-chain, and assignment/comma-result allocation tests pass, including resolved instance/static parameter contracts, annotated method returns, call-return propagation, conservative `if/else` joins, and agreeing/disagreeing ternary provenance. Parser JSON for recovered syntax is validated by Node and includes diagnostic spans. Generated-method source locations survive invalid-C compilation with local TCC, GCC, and Clang on Linux x86_64. The shared legacy/Tree-sitter construct-span gate passes for all 59 current standard-library `.cp`/`.c+` sources; the comparison trims trailing trivia from legacy scanner spans but preserves both backend results. Nested function-pointer parameters and two return-composition layers compile and execute with host `cc`. All 91 Tree-Sitter corpus cases pass, generated parser source matches regeneration, and `./gradlew test build :cli:fatJar --no-daemon` passes on Linux x86_64 / JDK 21. VS Code `npm test`, Vim headless parser checks for clean/malformed sources, IntelliJ parser-annotation unit tests, and IntelliJ `buildPlugin` pass. Recent focused checks cover C declarator/initializer/enumerator/attribute/designator AST categories, wrong-kind receiver diagnostics with mapped `.cp` spans, VS Code normalized-tree outlines, and Vim normalized-AST location lists including Unicode columns. Other host-matrix jobs remain unexecuted. `git diff --check` is clean for the current changes.
- Phase 2/3 JVM prototype verification: KTreeSitter 0.25.1 parses via JNI under Temurin 21 on Linux x86_64; CMake 3.28.3 / GCC 13.3.0 builds and loads the grammar bridge. The Linux x86_64 JVM jar is 149 KiB and includes a 1,283,272-byte JNI library. CI now uploads host-specific bridge resources, merges all six into CLI packaging, and asserts their paths in both release jars. The local jar contains and runs its Linux x86_64 bridge; remote artifact assembly and macOS/Windows/ARM64 loading are pending. No production parser selection is enabled.
- Phase 8 groundwork verification: `CPlusParseJson` serializes the same normalized AST and diagnostics independently of editor platform; the schema is versioned and explicitly declares UTF-16 offsets. Tests cover escaping, Unicode-containing buffers, category serialization for C declarations/control flow and comptime interpolation, and Node JSON parsing for both valid trees and recovered diagnostics. The CLI `parse` command supports saved files and `parse --stdin --source filename.cp` for live editor snapshots, returning recovered AST plus diagnostics and a nonzero status on syntax errors. VS Code sends unsaved text to stdin, debounces parser diagnostics while typing, and reuses a same-revision normalized tree for outline symbols (struct fields/methods and top-level functions) and Test Explorer fixture names/spans; compiler-backed diagnostics remain save-triggered, and scanner/index fallbacks remain available. Vim `:CPlusParse` and `:CPlusSymbols` both parse the live buffer through stdin; the headless fixture appends an unsaved declaration and confirms it appears in the AST outline. Vim's Unicode fixture verifies symbol categories and UTF-16-to-byte location conversion. Import graphs remain disk-backed: VS Code, IntelliJ, and Vim consume `cplus.imports.v1` through `cplus graph`, show resolved edges, and open selected imports; Vim's headless test verifies navigation and preservation of parser quickfix data. IntelliJ's opt-in external annotator parses the current unsaved buffer and retains its normalized tree in a bounded exact-source cache for member completion, Go to Declaration, Structure view, and test-gutter fixture discovery, with text fallbacks where supported. IntelliJ tests cover schema decoding, member filtering, stale snapshots, nested-symbol navigation, fixture spans, cache invalidation, and import-graph decoding. `npm test` in `vscode-cplus`, `./gradlew -p intellij-cplus test buildPlugin --no-daemon`, and `vim -Nu NONE -i NONE -n -es -S vim-cplus/test/outline.vim` pass. Broader editor feature parity remains open.
- Phase 7 source-map verification: the prototype emits `#line` directives from mapped origins; integration tests compile invalid lowered methods with each locally available TCC/GCC/Clang and check original `.cp` filenames and lines. The final experimental output passes through `CPlusAstCEmitter`, which emits normalized AST terminal tokens with deterministic block/statement layout and selected AST-context punctuation rules, preserves preprocessor regions and token origins, and reparses emitted output before returning it. Tests verify terminal/preprocessor token-stream round-trip, array and ternary formatting, semicolon handling inside `for` headers, compile/run emitted C, assert emitted token offsets, verify mapped compiler diagnostics, and confirm try/catch comments and catch statements retain exact origins. `./gradlew build --no-daemon` passes on Linux x86_64 / JDK 21, including all 91 grammar corpus cases. Full grammar-aware formatting, semantic node generation, and remote platform/toolchain matrix verification remain open.
- Phase 7 initial benchmark baseline (before incremental-session support): `./gradlew :parser-tree-sitter:benchmarkFrontend --no-daemon` passed on Linux x86_64, 16 logical CPUs, Temurin 21.0.9 (2026-09-26). One warmup and 3–5 samples measured a fresh full parse of an edited 37,316-byte largest repository source at 37.604 ms. The measurements are local directional baselines, not stable budgets.
- Phase 5/6 explicit receiver regression: type-qualified instance calls with a compatible explicit pointer receiver (for example `box_t.set(&box, 35)`) resolve separately from static calls and emit the receiver once. The prototype integration test compiles and executes the emitted C. This preserves the stdlib's existing `Type.method(&receiver, ...)` idiom while still diagnosing type-qualified instance calls without a matching receiver.
- Phase 5 file-scope receiver regression: semantic indexing seeds value scope from top-level declarations, descending through preprocessor conditionals and excluding typedefs. This lets global struct instances resolve implicit calls such as `xmem.init()` and lowers them to C function calls. A host integration test asserts the generated xmem call and compiles both `stdlib/io/file.cp` and `stdlib/memory/xmem.cp` output as C11 with each locally available `cc`, `gcc`, or `clang`.
- Phase 7 representative-source benchmark: on Linux x86_64 / Temurin 21.0.9, successful prototype transcodes include `stdlib/encodings/rune.cp`, `stdlib/http/protocol.cp`, `stdlib/io/file.cp`, `stdlib/memory/xmem.cp`, `examples/annotated.cp`, and `examples/basic.cp`; `file.cp` and `xmem.cp` also compile as C11 on each locally available host compiler. One run recorded respective transcode medians of 5.743, 134.171, 253.301, 414.864, 5.405, and 15.049 ms. The latest complete run measured a fresh full parse of the 37,316-byte edited xmem source at 37.424 ms and an incremental session update on the 37,295-byte original after a 21-byte insertion at 25.950 ms, reusing the old Tree-sitter tree and reporting one changed range. The update still rebuilds the normalized compiler AST, so this is not an editor end-to-end latency claim. These local results do not establish full-source semantic parity or cross-host performance.
- Phase 4 graph verification: compiler tests cover deterministic DAG edges, dependency-first ordering, cycle paths, shared-manager imported snapshots, root-import-site cycle diagnostics, relative and prefixed `stdlib:/`, `module:/`, and `project:/` imports, extensionless source resolution, and rejection of prefixed-root traversal. `TranscodedSource.sourceOrder` and `sourceImports` expose canonical dependency order and requesting edges. The `cplus graph` command serializes this metadata as `cplus.imports.v1`; CLI tests verify canonical dependency-first ordering and requester spans, while VS Code, IntelliJ, and Vim actions decode the protocol and open selected edge targets. The legacy evaluator remains the active materializer; AST-driven expansion remains open.
- Phase 4 comptime-index verification: nested generator bodies keep their inner generator/invocation nodes dormant, while `enclosingGeneratorSpan` identifies the nearest generator that must materialize them. The regression asserts the outer invocation remains active, inner nodes point to the outer declaration, and no evaluation or legacy behavior is changed.
- Phase 4 comptime signature/index verification: the AST index exposes modern result kinds and generic/function parameter metadata, legacy `@type`/`@fn` generic parameter names, exact invocation argument spans, and typedef specialization aliases. Focused JVM tests cover those fields; this remains syntax metadata only and does not change the legacy evaluator.
- Phase 4 conditional-lowering verification: the Tree-sitter prototype selects an `@else if` target branch, lowers nested `@if` and `!=` conditions across parse passes, preserves selected source origins, and rejects unsupported `arch` syntax even in a branch made unreachable by an earlier match, mapping the diagnostic to the original condition span. The focused regression and `./gradlew :parser-tree-sitter:jvmTest --no-daemon` pass. This remains an OS-only prototype slice; general comptime evaluation, architecture conditions, imports, and production backend switching are not implemented here.
- Phase 4 scalar-materialization verification: AST-backed tests materialize forward-referenced integer values, arithmetic, inline expressions, and short-circuiting; verify emitted literal origins and compile/run the emitted C; and reject division-by-zero, unsupported `double` values, and non-module-scope declarations at source-mapped spans. The legacy evaluator remains the production authority; scalar strings, comptime functions/generators, reflection, and imports are not migrated by this slice.
- Phase 4 compiler-flag verification: an OS-selected branch yields `-lraylib`, `-lm`, a quoted `-Wl,custom`, and a later `-pthread`; duplicate `-lm` is removed, the other platform's flags and source directive do not reach C, and function-local flags are rejected with a mapped diagnostic. Options are returned as a stable ordered list for later CLI integration.
- Phase 4 C-import migration verification: `TreeSitterCImportLowering` consumes the parsed `cplus_at_import` node, resolves relative and configured `stdlib:/` paths through the same `CPlusImportResolver` used by legacy comptime, emits a normal absolute `#include`, and retains the import token's original `.cp` origin. The generated program compiles and runs; missing imports report a mapped diagnostic. C-plus module imports now also have a bounded opt-in AST path; the production compiler still uses the legacy evaluator, and generator evaluation remains legacy-only.
- Phase 4 AST module-import verification: nested modern imports and legacy `.cp` `@import`s share the canonical resolver and graph, duplicate imports emit a module once, selected platform branches are resolved only after conditional materialization, imported scalar values participate in AST materialization, and import cycles report at the closing import's original file/span. Dependency-first `sourceOrder` and canonical edges are attached to `TranscodedSource`; generated C is compiled and run.
- Mapped-test regression: `MappedText.toOriginalSpan` is now the shared half-open-span mapping operation used by AST fixture extraction, so fixture/assertion metadata and diagnostics from reparsed/import-expanded text retain original source file names and offsets. Focused regressions cover root and imported-source fixture/assertion origins.
- Phase 5 comma-declarator regression: semantic variable-scope registration visits every declarator in file- and block-scope declarations, unwraps initialized declarators, retains per-declarator pointer depth, and excludes plain function declarations from object scope. The Tree-sitter test resolves methods on both `widget_t value, *pointer;` pairs at file and function scope plus a typed parameter; `./gradlew :parser-tree-sitter:jvmTest --tests 'cplus.parser.TreeSitterCPlusParserBackendTest.resolvesEveryObjectInCommaSeparatedFileAndBlockDeclarations' --no-daemon` passes. Broader qualified declarators and comptime-name resolution remain open.
- Phase 5 `for`-scope regression: the semantic visitor carries the initializer declaration into the condition, update, and body using a loop-local symbol map, without leaking it to following statements. The test asserts method resolution in each region and no resolution after the loop; the focused parser test passes.
- Phase 5 declarator-layer regression: semantic receiver types retain the C binding sequence (for example `[ARRAY, POINTER]` for arrays of pointers versus `[POINTER, ARRAY]` for pointers to arrays). Subscript operations consume the outer array/pointer layer instead of flattening all indirections into a count. Coverage includes local arrays, arrays of pointers, pointer-to-array, struct array/pointer fields, repeated indexing, and generated C compile/run via the prototype; focused `resolvesIndexedReceiversAcrossArrayPointerDeclaratorShapes` passes. C qualifiers, less-common attributes/declarators, and comptime-name resolution remain open.
- Phase 5 receiver-operator/declarator-shape regression: known value receivers reject `->`, one-level pointers reject `.`, and pointer-to-pointer values require explicit dereference; the existing `(&value).method()` form remains accepted. Direct array receivers allow C array decay only for arrays of structs; arrays of pointers and pointers to arrays are diagnosed instead of being mistaken for direct struct pointers. Tests verify diagnostic spans and fail-closed prototype behavior, and compile/run the valid array-decay generated C with the host compiler. Focused verification: `./gradlew :parser-tree-sitter:jvmTest --tests 'cplus.parser.TreeSitterCPlusParserBackendTest.distinguishesDirectArrayDecayFromNestedArrayPointerReceivers' --no-daemon` passes.
- Phase 5 semantic-index verification: tests cover struct/method and top-level function symbols, access and ownership/mutation metadata, explicit instance/static call resolution, inferred `self` receiver resolution inside methods, parenthesized address/dereference receiver typing and correct already-pointer argument lowering, chained field receiver lookup from declared struct field types, named alias chains, const-qualified aliases, pointer typedefs with depth retained across alias chains, pointer-to-pointer rejection unless explicitly dereferenced, anonymous struct typedef ownership and method resolution, typed parameters, nested local shadowing, distinct `@throws()` / `@throws(error)` conventions with error-out parameter placement, and catch code/binding extraction. A dedicated comptime-scope regression verifies both modern and legacy generators plus typedef specializations stay out of runtime symbols until materialization. Function-pointer typedefs retain callable signature metadata (base result, parameter names/base types and source spellings, variadic state, declarator span) plus the complete original typedef declaration, including pointer/qualifier spelling. Callback parameters recurse and two nested function-pointer return-composition layers are indexed; the C11 interoperability regression compiles and runs the fixture independently through locally installed `cc`, `gcc`, and `clang`. Cross-host/target coverage, deeper C declarator normalization, and comptime name/scope resolution remain open.
- Phase 6/7 foundation verification: `TreeSitterCPlusPrototypeTranspiler` composes AST-bounded `@test` extraction, throws extraction/removal, statement-oriented checked-call/try-catch lowering, defer, receiver-call, struct-method passes, bounded OS-conditional lowering, and integer/bool scalar materialization, reparsing between stages while retaining `MappedText` origins. Fixture/annotation removal, defer placement, struct-method extraction, receiver-call lowering, and checked-call/try-catch transformations use node-keyed mapped emission; nested receiver calls recursively lower inside the owning call node, and nested tries/checked statements are replaced inside mapped subregions. Runtime C11 tests compile and execute nested error propagation. Compiler tests verify trivia and original/generated origin preservation. Test extraction retains decoded names, mapped bodies/spans, and AST-indexed assertions; the existing harness compiles and executes extracted fixtures. The legacy comptime fixture route still uses its scanner. AST defer handles nested control flow with function-wide LIFO placement. The opt-in allocation analyzer covers direct allocators, simple aliases/assignments, global parameter/return contracts, owned double-pointer outputs, resolved instance/static method parameter contracts, annotated method returns, direct call-return propagation, equal-provenance `if/else` joins, and same-domain ternary arms; broader expression and branch-sensitive dataflow remain open. Host C11 integration covers the supported lowering subset and source-mapped diagnostics. The prototype still fails closed on general comptime generators/invocations, non-scalar comptime values, imports, and C-plus imports; production `CPlusTranspiler` remains authoritative.
- Phase 6 pipeline verification: the experimental runtime-lowering sequence now runs through `CPlusAstLoweringPipeline`. A parser integration test confirms deterministic pass order, reparsing after source-changing passes, AST/source revision agreement at every stage, preserved original source origins across generated revisions, and fail-fast behavior after lowering diagnostics. `gradle :parser-tree-sitter:jvmTest --no-daemon` passes. This is orchestration over normalized AST passes, not yet a dedicated typed lowered-node IR.
- Phase 6 allocation-analysis verification: targeted JVM tests exercise direct and aliased AST-domain mismatches, positive matching provenance, direct reassignment, global-function parameter checks at call sites, return contracts merged across prototype/definition pairs, owned double-pointer output writes and caller-side invalidation (including output calls nested in switch branches), resolved implicit/static/explicit-instance method parameter checks, annotated method return contracts and source spans, function/method call-return provenance at argument sites, equal-domain joins across distinct allocation sources, divergent `if/else` and ternary outcomes becoming unknown, assignment side effects, sequenced comma writes, possibly skipped `&&`/`||` writes becoming unknown, invalidation after potentially side-effecting call arguments, and `while`/`for`/`do`/`switch` outer-variable invalidation while retaining loop-local scope. General unsequenced binary operand combinations and loop fixed-point/broad C expression semantics remain open; the legacy analyzer remains the production path.
- Required commands for each compiler change: `./gradlew :compiler:test`, `./gradlew test`, and `./gradlew build` when public packaging or module wiring changes.
- Each parser phase adds small targeted tests before broad fixtures. Record tested platform/JDK/binding versions in this file when native integration begins.
- Do not mark a row complete based only on compilation: include behavior parity, span checks, and regression-suite results.
